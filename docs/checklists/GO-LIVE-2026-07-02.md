# 🚀 Plano de Go-Live — 02/07/2026 (manhã)

**Orquestrador:** @aiox-master
**Alvo:** rhcursos.com.br em produção no Cloudflare Workers
**Estado verificado em 01/07 (noite):** lint ✅ · typecheck ✅ · build ✅

## Evidência de partida

| Item | Status |
|------|--------|
| `npm run lint` | ✅ Passou |
| `npm run typecheck` | ✅ Passou |
| `npm run build` | ✅ Passou (todas as rotas geradas) |
| Diff não commitado | ⚠️ Redesign header/footer/Home + snapshots ui-governance |
| Branch | `main` (confirmar se deploy sai de `main`) |
| Deploy | `npm run deploy:workers` + `npm run verify:workers` |

---

## Fase 0 — Congelar trabalho pendente (hoje à noite, ~30min)

**Agente:** @dev (Dex) · **Modelo:** Sonnet (executor)

- [x] Revisar diff de `public-header.tsx`, `public-footer.tsx`, `Home.tsx`
- [x] Rodar `npm run test:e2e:smoke` para validar snapshots atualizados
- [x] Commit atômico: `feat(design): apply Trust Keith to public shell and home`
- [x] **Feature freeze** — nenhuma mudança de escopo após este commit

## Fase 1 — QA Gate (hoje à noite, ~45min)

**Agente:** @qa (Quinn) · **Modelo:** Sonnet · Escalar para Opus só se houver falha não trivial

- [x] `npm run test:unit` — 392 passed / 0 failed
- [x] `npm run test:e2e:smoke` (functional) — 76 passed / 1 skipped (intencional, agenda)
- [x] `npm run test:a11y` — 9 passed, WCAG 2.1 A/AA ok em todas as rotas públicas
- [x] Verdict: **PASS** (QA Gate liberado em 01/07, madrugada)

## Fase 2 — Revisão de segurança pré-produção (hoje à noite, ~30min)

**Agente:** security-reviewer · **Modelo:** Opus (única fase que justifica Opus preventivo)

- [x] `NEXT_PUBLIC_ENABLE_DEMO_AUTH` — flag morta, sem consumidor no código (demo auth removido)
- [x] Nenhum secret commitado — working tree e histórico limpos
- [x] `AUTH_SESSION_SECRET` — validação ≥32 chars em 3 camadas; gerar valor prod com CSPRNG na Fase 3 (mesmo valor no Worker e nas Edge Functions)
- [x] RLS trancado (lead/aluno/inscricao owner-or-admin; policies permissivas antigas dropadas em sprint1_security)
- [x] Headers de segurança em `next.config.mjs` + validados por `verify:workers` (7 headers)
- [x] Verdict: **APPROVED com CONCERNS** — zero bloqueadores; dívidas pós-launch: endurecer CSP (`unsafe-eval`/`unsafe-inline`), Zod na Edge Function `leads`, limpar flags demo do `.env.example`, revisar upsert anônimo de `aluno` no RPC de inscrição

## Fase 3 — Preparação de produção (amanhã cedo, ~40min)

**Agente:** @devops (Gage) — autoridade EXCLUSIVA · **Modelo:** Sonnet

- [x] `npx wrangler whoami` — autenticado (conta `pedro.guts@gmail.com`, account `73bc4dd9e913ab1e10e9e1647e1229b3`)
- [x] Secrets no Worker (`wrangler secret list`): `AUTH_SESSION_SECRET` e `SUPABASE_SERVICE_ROLE_KEY` presentes. `NEXT_PUBLIC_SENTRY_DSN` e `NEXT_PUBLIC_GA_MEASUREMENT_ID` ausentes (opcionais — decisão humana se ativar antes do go-live). `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` não são secrets do Worker (são inline em build-time via CI) — ver bloqueador crítico abaixo.
- [ ] **BLOQUEADOR CRÍTICO:** 0 secrets configurados no GitHub Actions (verificado via API em repo e no ambiente `production`) — `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `AUTH_SESSION_SECRET`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `NEXT_PUBLIC_APP_URL` — todos ausentes. `deploy-frontend.yml` vai falhar antes mesmo de chamar o wrangler. Ação humana obrigatória (`gh secret set` ou GitHub UI).
- [ ] Paridade `AUTH_SESSION_SECRET` Worker ↔ Supabase Edge Functions: confirmado presente no Worker; não verificável no lado Functions — `npx supabase secrets list` retornou 401 (sem `SUPABASE_ACCESS_TOKEN` neste ambiente). Ação humana: rodar com token válido e confirmar presença dos dois lados (valores nunca são exibidos pelo CLI).
- [ ] Confirmar projeto Supabase de PRODUÇÃO — migrations aplicadas: CLI linkado localmente (ref `hwpsrujkxjhmmwphqdlzto`) mas não autenticado (`supabase migration list` → 401 Unauthorized). 14 migrations locais até `20260701090000_instructor_portal_rls.sql`; status remoto não verificável sem `SUPABASE_ACCESS_TOKEN`.
- [ ] `npm run build:workers` local — **FALHOU.** `next build` completa normalmente (47 rotas geradas), mas o adapter OpenNext Cloudflare aborta no pós-processamento com `ERROR Node.js middleware is not currently supported. Consider switching to Edge Middleware.` Causa provável: `proxy.ts` (middleware renomeado no Next 16) sem `export const runtime = "edge"`. Correção é código — fora do escopo @devops, encaminhar para @dev antes de repetir o build.
- [ ] `npm run preview:workers` — **SKIPPED.** Build não gerou `.open-next/worker.js`; nada para servir em preview.
- [ ] `git push origin main` — **NÃO EXECUTADO (decisão deliberada @devops).** `main` local está 1 commit à frente de `origin/main` (`1f1f0d9`, que altera `src/**`) e `deploy-frontend.yml` dispara automaticamente em push para `main` tocando esses paths — ou seja, o push acionaria a Fase 4 mesmo estando fora de escopo aqui, sobre um build quebrado e sem secrets no GitHub. Mantido bloqueado até (a) `proxy.ts` ser corrigido, (b) secrets do GitHub Actions serem configurados, e (c) Fase 4 ser explicitamente autorizada.
- [x] `npm audit --omit=dev` — 2 vulnerabilidades moderate (`postcss <8.5.10`, via `next`), 0 high/critical.

## Fase 4 — Deploy + verificação (amanhã cedo, ~30min)

**Agente:** @devops (Gage) · **Modelo:** Sonnet · Falha de deploy → debugger com Opus

- [ ] **BLOQUEADOR CRÍTICO — deploy FALHOU.** `npx opennextjs-cloudflare deploy` (após `npm run build:workers` bem-sucedido, 47 rotas) subiu os 51 assets estáticos, mas a API do Cloudflare rejeitou a versão do Worker: `Your Worker exceeded the size limit of 3 MiB` (código 10027). Medido localmente: `.open-next/server-functions/default/handler.mjs` = 13.02 MiB brutos / **3.29 MiB gzip** (acima do limite do plano Free); somando `.open-next/middleware/handler.mjs` (169.79 KiB gzip) o total ultrapassa ainda mais a margem. `npx wrangler whoami` confirma a conta como está hoje — sem plano Workers Paid ativo (o próprio erro do Cloudflare sugere upgrade para o plano pago, que eleva o limite para 10 MiB). **Nenhuma versão nova foi publicada** — a validação falha antes da promoção, então a versão anterior (`9cc44317-c703-4394-b1c0-be98e21d70c0`, `wrangler deployments list`) permanece ativa e servindo tráfego. Confirmado via `www.rhcursos.com.br` → HTTP 200 antes e depois da tentativa. **Rollback não necessário** (nada mudou em produção). Ação humana necessária — duas rotas possíveis, não mutuamente exclusivas: (a) upgrade da conta Cloudflare para Workers Paid ($5/mês, decisão de billing, fora da autoridade @devops); (b) investigação @architect/@dev sobre por que `handler.mjs` está em 13 MiB (bundle de todas as 47 rotas server-side; suspeita de dependências pesadas ou tree-shaking insuficiente no adapter OpenNext — não identificada causa raiz específica nesta sessão).
- [x] `npm run verify:workers` (`DEPLOY_CANONICAL_URL=https://www.rhcursos.com.br`) — rodou contra a versão **antiga** ainda no ar (nenhum deploy novo foi publicado). Resultado: ❌ falhou por header ausente `Content-Security-Policy` — esperado, pois a versão em produção é anterior às mudanças de `next.config.mjs` que adicionam esse header; não é uma regressão desta tentativa de deploy.
- [x] Smoke manual em produção (via curl, versão antiga ainda no ar):

  | Rota | Status | Observação |
  |------|--------|------------|
  | `/` | 200 | title/h1 OK |
  | `/cursos` | 200 | title/h1 OK, listagem com slugs reais |
  | `/blog` | 404 | rota `app/blog` existe no código local (HEAD `726f67c`) mas não está na versão publicada — esperado, não é regressão nova |
  | `/contato` | 200 | formulário de contato carrega (título "Entre em Contato" + h1); **nenhum lead enviado**, conforme instrução |
  | `/login` | 200 | title/h1 OK |
  | `/sobre` | 200 | title/h1 OK |
  | `/cursos/atendimento-ao-publico-...-gestao-de-conflitos` (slug real extraído da listagem) | 200 | página de curso renderiza título e h1 |
  | `/admin` (sem sessão) | 307 → `/login?next=%2Fadmin` | comportamento correto |

- [ ] **Apex → www: DIVERGÊNCIA.** `https://rhcursos.com.br/` responde **200 direto** (sem redirect 301/308 para `www`). Esperado seria 301/308. Reportado como divergência — não bloqueia (site funcional em ambos os hosts), mas deveria ser corrigido (regra de redirect ausente/incorreta na config de DNS/Workers routes do Cloudflare).
- [ ] Sentry recebendo eventos · GA4 ativo — **N/A nesta sessão.** `NEXT_PUBLIC_SENTRY_DSN` e `NEXT_PUBLIC_GA_MEASUREMENT_ID` não configurados (confirmado na Fase 3); decisão humana pendente sobre ativar antes do go-live real.

**Push (`git push origin main`) — NÃO EXECUTADO.** Condição do orquestrador era "com o deploy verificado" — o deploy não foi verificado (falhou). `main` local segue 3 commits à frente de `origin/main` (`1f1f0d9`, `a0011fa`, `726f67c`, todos já commitados anteriormente). Mantido bloqueado até o bloqueador crítico acima ser resolvido (upgrade de plano ou redução de bundle) e um deploy bem-sucedido ser verificado.

## Fase 5 — Pós-deploy (amanhã, primeira hora no ar)

**Agente:** @devops + @qa · **Modelo:** Haiku (monitoramento é mecânico)

- [ ] Monitorar Sentry por 1h (zero erros novos críticos)
- [ ] Cloudflare Analytics: taxa de erro < 1%
- [ ] `seed:admin` executado se necessário (acesso admin em prod)
- [ ] **Rollback:** `wrangler rollback` (Workers mantém versões anteriores) — decisão em < 5min se erro crítico

---

## Roteamento de modelos (gestão de tokens)

| Trabalho | Modelo | Racional |
|----------|--------|----------|
| Commits, checklist, monitoramento | Haiku | Mecânico, zero raciocínio profundo |
| Testes, fixes pontuais, deploy ops | Sonnet | Padrão custo/benefício |
| Security review, debug de falha de deploy | Opus | Único ponto onde erro custa o go-live |
| Orquestração | Sessão principal | Não re-executa evidência já coletada |

**Regras de economia:** builds/testes em background; nunca repetir verificação já verde na mesma fase; escalar Opus apenas sob falha; um único full-run (`npm run test`) antes do deploy, não vários parciais redundantes.

## Fora de escopo (não bloqueia go-live)

- Fases restantes do DESIGN_REFACTOR_PLAN (tokens ~80 vars, cobertura 100%) → epic pós-launch
- Lighthouse/bundle-check → rodar pós-launch como baseline
- Storybook build → não é artefato de produção
