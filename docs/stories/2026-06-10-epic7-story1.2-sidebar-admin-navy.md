# Story EP-1.2: Sidebar Admin Navy Executive Precision

## Status
Done

## Epica
Epico 7 — Redesign "Executive Precision" (`docs/epics/epic-7-redesign-executive-precision.md`, §4 FASE 1)
Spec aprovada: `docs/specs/epic-7-executive-precision/spec.md` — FR-8, NFR-1, NFR-3, CON-1, CON-3, CON-4
Fonte canonica de design: `docs/design/executive-precision/DESIGN.md` + `screens/admin-*.html`
Branch alvo: `redesign/ep-0-fundacao`

## Contexto

Complementa a EP-1.1 ativando o tema Executive Precision no shell admin. O epico (§2) determina sidebar admin de cinza para navy `#083B56`, com item ativo em gold e contraste AA. A ativacao ocorre aplicando `data-theme="executive"` no contêiner raiz do `DashboardShell` (`src/features/admin-shell/dashboard-shell.tsx`), reentoando `admin-sidebar.tsx` por tokens. A navegacao por teclado e os landmarks (`aside`, `nav`) devem permanecer intactos.

## Acceptance Criteria

- [x] AC1 — `DashboardShell` aplica `data-theme="executive"` no contêiner raiz, ativando a identidade nas rotas admin sem alterar o contrato do shell (FR-8).
- [x] AC2 — `AdminSidebar` passa a navy Executive Precision (superficie navy escuro), preservando logo, perfil, navegacao e acoes; landmarks e operacao por teclado preservados (FR-8, NFR-3).
- [x] AC3 — Item de navegacao ativo usa destaque gold com contraste AA do texto sobre o realce; itens inativos legiveis sobre o navy (FR-8, NFR-1).
- [x] AC4 — Nenhuma variante `dark:` ou `[data-theme="dark"]` nova; backend/Supabase intocado (CON-4, CON-5).
- [x] AC5 — Gate axe das rotas admin (guard/login baseline) preservado; baseline visual rebaselined se afetado, em commit separado (CON-1, NFR-1).
- [x] AC6 — `npm run lint`, `npm run typecheck`, `npm test` verdes.
- [x] AC7 — File List e Change Log atualizados.

## Scope

### In Scope

- Ativar `data-theme="executive"` no `DashboardShell`.
- Reentoar `admin-sidebar.tsx` para navy + item ativo gold, por tokens.

### Out of Scope

- Reskin de conteudo das paginas admin (dashboard, cursos, alunos) — Fase 5.
- Header/footer publicos e FAB — EP-1.1.
- Google Analytics 4 — EP-1.3.
- Backend/Supabase.

## Tarefas / Subtarefas

- [x] Aplicar `data-theme="executive"` no contêiner raiz do `DashboardShell`.
- [x] Reentoar sidebar para navy escuro, item ativo gold com contraste AA.
- [x] Verificar landmarks/teclado preservados e ausencia de `dark:` novo.
- [x] Re-baseline de baseline visual se afetado (commit separado).
- [x] Rodar `npm run lint`, `npm run typecheck`, `npm test`.
- [x] Atualizar File List e Change Log.

## Dependencies

- EP-1.1 (ativacao do tema no shell publico) e FASE 0.

## Definition of Done

- Todos os AC marcados.
- Sidebar admin navy com item ativo gold AA; teclado/landmarks preservados.
- Gates verdes.
- File List e Change Log atualizados.

## File List

### Criados
- `docs/stories/2026-06-10-epic7-story1.2-sidebar-admin-navy.md`

### Modificados
- `src/features/admin-shell/dashboard-shell.tsx`
- `src/features/admin-shell/components/admin-sidebar.tsx`

## Dev Agent Record

### Agent Model Used
Claude Opus 4.8

### Completion Notes
- `DashboardShell` aplica `data-theme="executive"` no contêiner raiz, ativando a identidade nas rotas admin.
- `AdminSidebar` passou de `bg-surface-muted` (cinza) para navy escuro (`var(--ea-footer-bg,#083b56)`), com bordas `white/10`, textos em branco/`white/70` e logo em caixa branca para contraste.
- Item de navegacao ativo usa realce gold (`--ea-color-prestige-gold`) com texto navy (`--ea-button-primary-fg`) — par AA validado na F0; itens inativos em `white/70` sobre navy.
- Botoes do rodape da sidebar (Ver cursos / Sobre / Sair) ganharam override de contraste sobre navy (`border-white/20`, `text-white`) preservando as variantes `outline`/`ghost`.
- Landmarks (`aside`, `nav`) e operacao por teclado preservados; nenhum `dark:` novo; backend intocado.

## Change Log

- 2026-06-10 — @sm (River) — Story criada a partir do Epic 7 FASE 1 (EP-1.2) cobrindo sidebar admin navy com item ativo gold.

## QA Results

**Data:** 2026-06-11 · **Agente:** @qa (Quinn) · **Task:** `qa-gate` · **Veredito: PASS**

### 7 verificações de qualidade

| # | Check | Resultado | Evidência |
|---|-------|-----------|-----------|
| 1 | Code review | PASS | `data-theme="executive"` no `DashboardShell`; sidebar navy (`--ea-footer-bg`), item ativo gold/navy, botões do rodapé com override de contraste. |
| 2 | Testes | PASS | `npm test` = 89 passed; `route-auth` (guard admin) verde. |
| 3 | Acceptance criteria | PASS | AC1-AC7: tema admin ativo, sidebar navy, item ativo gold AA, landmarks/teclado preservados. |
| 4 | Sem regressões | PASS | Guard server-side e navegação admin preservados. |
| 5 | Performance | PASS | Apenas classes/tokens. |
| 6 | Security | PASS | Sem `dark:` novo; backend intocado. |
| 7 | Documentação | PASS | Completion Notes detalham navy + item ativo gold. |

**Nota:** par gold/texto-navy do item ativo é o validado em AA na F0. Status Ready for Review → Done.
