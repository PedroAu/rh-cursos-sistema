# Story 2.4: Resolve or track Next/PostCSS advisory

## Status

Done

## Executor Assignment

executor: "@devops"
quality_gate: "@qa"
quality_gate_tools:
- npm run lint
- npm run typecheck
- npm test
- npm run build:cloudflare

## Story

**As a** maintainer,  
**I want** dependency vulnerabilities triaged and patched or explicitly waived,  
**so that** releases have a known dependency risk posture.

## Acceptance Criteria

1. `npm audit --audit-level=moderate` is re-run and result is captured.
2. If a patched compatible Next version exists, dependencies are upgraded and tests/build pass.
3. If no compatible patched version exists, a documented waiver is created with rationale, impact, monitoring trigger, and expiration date.
4. `package-lock.json` is updated only when a real dependency change is made.

## Source of Truth

- `docs/security/site-security-audit-2026-06-21.md`
- `package.json`
- `package-lock.json`

## Tasks / Subtasks

- [x] Re-run dependency audit.
- [x] Check patched Next availability.
- [x] Upgrade or create waiver.
- [x] Run required quality gates.

## Dev Notes

- Do not downgrade Next to satisfy `npm audit fix`.
- This repo uses Next 16.2.9; verify compatibility before upgrading.

## Testing

- `npm audit --audit-level=moderate`
- If upgraded: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build:cloudflare`.

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-06-21 | 0.1 | Story drafted from PM security hardening plan | River |
| 2026-06-21 | 0.2 | PO validation GO, P2 priority confirmed | Pax |
| 2026-06-21 | 1.0 | Re-ran audit, confirmed no patched stable Next 16 release, and created temporary waiver | Gage |
| 2026-06-21 | 1.0.1 | QA Gate PASS (WAIVED advisory) — Status: Ready for Review → Done | @qa (Quinn) |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `npm audit --audit-level=moderate`: FAIL (2 moderate vulnerabilities; `next -> postcss <8.5.10`; `npm audit fix --force` would install breaking downgrade `next@9.3.3`)
- `npm view next version`: `16.2.9`
- `npm view next@latest version dependencies.postcss`: `next@16.2.9`, `postcss@8.4.31`
- `npm view next@16 version --json`: latest stable 16.x is `16.2.9`
- `npm run lint`: PASS
- `npm run typecheck`: PASS
- `npm test`: PASS (39 files, 140 tests)
- `npm run deploy:check`: PASS (39 files, 140 tests, Cloudflare build complete; rerun outside sandbox after Turbopack sandbox bind restriction)

### Completion Notes List

- No patched compatible stable Next 16 release is available at review time.
- Rejected `npm audit fix --force` because it would downgrade Next to `9.3.3`.
- Created a temporary waiver with rationale, impact, monitoring trigger, and expiration.
- `package-lock.json` was not updated because no dependency change was made.

### File List

- `docs/security/site-security-audit-2026-06-21.md`
- `docs/security/waivers/next-postcss-ghsa-qx2v-qp2m-jg93-2026-06-21.md`
- `docs/stories/2.4.resolve-next-postcss-advisory.md`

## QA Results

### Review Date: 2026-06-21

### Reviewed By: Quinn (Test Architect)

### Verdict: PASS

The story's deliverable is a *risk decision* (upgrade or waive). QA re-ran the audit live and confirms the waiver path is correct and complete.

#### Acceptance Criteria Traceability

| AC | Status | Evidence |
| --- | --- | --- |
| AC1 — audit re-run & captured | PASS | QA re-ran `npm audit --audit-level=moderate`: 2 moderate (`postcss <8.5.10` via `next`). Result captured in waiver "Current Evidence". |
| AC2 — upgrade if patched version exists | PASS (N/A path) | Latest stable Next is `16.2.9`, still depends on `postcss 8.4.31` (in range). No patched compatible release exists → upgrade path correctly not taken. |
| AC3 — documented waiver if no patch | PASS | `docs/security/waivers/next-postcss-ghsa-qx2v-qp2m-jg93-2026-06-21.md` has rationale, impact, monitoring trigger, and expiration (2026-07-21). |
| AC4 — lockfile updated only on real change | PASS | No dependency change made; `package-lock.json` untouched. Confirmed in Completion Notes. |

#### 7 Quality Checks

1. **Code review** — PASS. No code change; correctly rejected `npm audit fix --force` (would downgrade to `next@9.3.3`, breaking).
2. **Unit tests** — N/A (no code change). Suite green regardless (147/147).
3. **Acceptance criteria** — PASS (all 4).
4. **No regressions** — PASS. Zero dependency/code change.
5. **Performance** — N/A.
6. **Security** — PASS (with accepted residual risk). Moderate PostCSS XSS in CSS stringify; site does not process attacker-controlled CSS. Waiver impact analysis is sound. This is effectively a **WAIVED** advisory wrapped in a PASS gate.
7. **Documentation** — PASS. Waiver is thorough and time-boxed with a concrete monitoring trigger.

#### Non-blocking Observations (low)

- **DEP-001 (low):** Waiver expires **2026-07-21**. A monitoring trigger is documented but not automated — recommend a calendar/CI reminder to re-run the audit before expiry.

### Gate Status

Gate: PASS → docs/qa/gates/2.4-resolve-next-postcss-advisory.yml
