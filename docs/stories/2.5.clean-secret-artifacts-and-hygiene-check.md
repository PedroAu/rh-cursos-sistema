# Story 2.5: Clean secret artifacts and add hygiene check

## Status

Ready for Review

## Executor Assignment

executor: "@devops"
quality_gate: "@qa"
quality_gate_tools:
- git status --ignored
- npm run lint
- npm run typecheck
- npm test

## Story

**As a** maintainer,  
**I want** local secret artifacts cleaned and checked,  
**so that** secrets are less likely to leak through commits, packaged artifacts, or shared workspaces.

## Acceptance Criteria

1. `.env.example` is confirmed sanitized.
2. Generated `.next/standalone/.env` is removed from local artifacts or documented as intentionally generated and excluded from packaging.
3. A secret hygiene check is documented or scripted for release/deploy preparation.
4. Git status/history check confirms no `.env` secrets are tracked.

## Source of Truth

- `docs/security/site-security-audit-2026-06-21.md`
- `.gitignore`
- `.env.example`

## Tasks / Subtasks

- [x] Review `.env.example` for placeholder-only values.
- [x] Remove or document generated env artifacts.
- [x] Add release/deploy secret hygiene check.
- [x] Run required quality gates.

## Dev Notes

- Never print secret values in logs, docs, tests, or final output.
- Rotate any secret if evidence shows it was committed or shared.

## Testing

- `git status --ignored`
- Optional secret scanner if available.
- Run standard project gates if repo files change.

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-06-21 | 0.1 | Story drafted from PM security hardening plan | River |
| 2026-06-21 | 0.2 | PO validation GO, P2 priority confirmed | Pax |
| 2026-06-21 | 1.0 | Removed generated env artifact and added release secret hygiene check | Gage |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `.env.example`: reviewed without printing secret values; secret-like entries are placeholder-only.
- `git ls-files '.env*' '.next/**/.env'`: only `.env.example` is tracked.
- `find .next -path '*/.env' -print`: found `.next/standalone/.env`; removed as a generated local artifact.
- `npm run security:secrets`: PASS
- `git status --ignored --short`: PASS (only path/status output reviewed; `.env` and `.env.local` are ignored, not tracked)
- `npm run lint`: PASS
- `npm run typecheck`: PASS
- `npm test`: PASS (39 files, 140 tests)
- `npm run deploy:check`: PASS (39 files, 140 tests, Cloudflare build complete; rerun outside sandbox after Turbopack sandbox bind restriction)
- `npm run security:secrets`: PASS after Cloudflare build

### Completion Notes List

- Removed local generated `.next/standalone/.env` artifact.
- Added `npm run security:secrets` release/deploy hygiene check.
- Documented secret hygiene commands and handling rules in `docs/security/secret-hygiene-check.md`.
- No real secret values were printed in logs or docs.

### File List

- `docs/security/secret-hygiene-check.md`
- `docs/security/site-security-audit-2026-06-21.md`
- `docs/stories/2.5.clean-secret-artifacts-and-hygiene-check.md`
- `package.json`
- `scripts/check-secret-hygiene.mjs`

## QA Results

_To be filled by QA._
