# Story 1.10: Add guardrails for Cloudflare/OpenNext build path

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools:
- npm run lint
- npm run typecheck
- npm test
- npm run build:cloudflare

## Story

**As a** developer preparing deploys,  
**I want** project scripts and documentation to make the Cloudflare/OpenNext build path explicit,  
**so that** nobody treats plain `next build` or static Cloudflare Pages export as the production artifact.

## Acceptance Criteria

1. Existing build/deploy scripts are reviewed for ambiguity around production deploy artifacts.
2. Documentation clearly states that Cloudflare deployment uses OpenNext Workers.
3. `npm run deploy:check` or equivalent validation remains the recommended pre-deploy command.
4. Any script or doc wording that implies Vercel/static export production deploy is corrected.
5. The change does not alter runtime deployment configuration unless a concrete mismatch is found.
6. Validation commands run or skipped commands are documented with reasons.

## Source of Truth

- `docs/architecture/system-architecture.md#Current Technical Debt and Known Issues`
- `docs/deploy.md`
- `package.json`
- `open-next.config.ts`
- `wrangler.jsonc`

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Deployment  
**Secondary Type(s)**: Documentation  
**Complexity**: Low

### Specialized Agent Assignment

**Primary Agents**:
- @dev
- @qa

**Supporting Agents**:
- @architect only if deploy-path guardrails require an architectural decision
- @devops only for remote push/PR after local completion, and supporting on deploy guardrail validation

### Quality Gate Tasks

- [x] Pre-Commit (@dev): verify deploy/build wording matches `package.json` scripts and the `deploy:check` reference before marking story ready for review.
- [ ] Pre-Review (@qa): verify story acceptance criteria and that no runtime deployment configuration changed without a concrete mismatch.
- [ ] Pre-PR (@devops): run repository quality gates before creating a pull request.

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (full mode per `coderabbit_integration.self_healing`)
- Max Iterations: 3
- Timeout: 30 minutes
- Severity Filter: CRITICAL and HIGH auto-fix; MEDIUM documented as debt; LOW ignored

**Predicted Behavior**:
- CRITICAL issues: auto-fix before story completion
- HIGH issues: auto-fix before story completion

### CodeRabbit Focus Areas

- Documentation must state Cloudflare deployment uses OpenNext Workers, not Vercel or static Cloudflare Pages export.
- Any wording implying Vercel or static export as the production deploy path must be corrected.
- `npm run deploy:check` must remain the recommended pre-deploy validation command.
- Do not alter runtime deployment configuration (`open-next.config.ts`, `wrangler.jsonc`) unless a concrete mismatch is found during review.
- This story complements Story 1.6 but focuses specifically on build/deploy guardrails, not general README accuracy.

## Tasks / Subtasks

- [x] Review package scripts and deploy docs.
- [x] Correct ambiguous build/deploy wording.
- [x] Add guardrail documentation for Cloudflare Workers/OpenNext.
- [x] Verify deploy check command references remain accurate.
- [x] Run relevant quality gates.

## Dev Notes

- This story complements Story 1.6 but focuses specifically on build/deploy guardrails.
- Do not push or deploy; @devops owns remote operations.

### Architecture Context

- The app is deployed to Cloudflare Workers via OpenNext, not static Cloudflare Pages export or Vercel. [Source: `docs/architecture/system-architecture.md#Current Technical Debt and Known Issues`]
- `package.json` defines `build` (`next build`, framework-level build only) and `build:cloudflare` (`opennextjs-cloudflare build`, the actual production artifact) as separate scripts; the distinction must stay unambiguous in docs and script wording. [Source: `package.json`]
- `deploy:check` chains `lint && typecheck && test && build:cloudflare` and is the recommended pre-deploy validation command. [Source: `package.json`]
- `docs/deploy.md` already documents the deploy flow; verify it does not imply Vercel or static export as the production target. [Source: `docs/deploy.md`]
- `open-next.config.ts` and `wrangler.jsonc` define the actual Cloudflare Workers runtime configuration; this story should not change them unless a concrete mismatch with documentation is found. [Source: `open-next.config.ts`, `wrangler.jsonc`]

### Expected Files

- `docs/deploy.md`
- `package.json` (script wording only, no behavior change unless mismatch found)
- `README.md` or related docs referencing build/deploy commands, if ambiguous wording is found

### Project Structure Notes

- `devStoryLocation` is configured as `docs/stories` in `.aiox-core/core-config.yaml`.
- `devLoadAlwaysFiles` points to `docs/framework/*`, but those files are missing in this repo. Use the available architecture document and local code as current source of truth.
- ClickUp sync from the create-next-story workflow was not performed because no ClickUp MCP/tool is available in this session.

### Constraints

- Do not implement application code outside this story scope.
- Do not alter `open-next.config.ts` or `wrangler.jsonc` runtime deployment configuration unless a concrete mismatch is found.
- Do not perform remote git operations; only @devops may push or create PRs.

## Testing

- Verify deploy:check command reference remains accurate against package.json.
- Run relevant gates; document any gate run or skipped with the reason.
- Run: `npm run lint`, `npm run typecheck` (and `npm run build:cloudflare` only if build wording/scripts changed).

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-06-19 | 0.1 | Initial draft from Architect brownfield handoff | River |
| 2026-06-19 | 0.2 | Expanded to canonical template (Executor Assignment, CodeRabbit Integration, Testing, Change Log, Dev Agent Record) | River |
| 2026-06-19 | 0.3 | Validated GO (8/10) — PO gate passed; references verified; Status: Approved | @po (Pax) |
| 2026-06-19 | 1.0 | Added explicit Cloudflare/OpenNext build guardrails to deploy docs | Dex |
| 2026-06-21 | 1.0.1 | QA Gate PASS — Status: Ready for Review → Done | @qa (Quinn) |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- Reviewed `package.json`, `docs/deploy.md`, `README.md`, `open-next.config.ts`, and `wrangler.jsonc`.
- Verified scripts match expected values: `build`, `build:cloudflare`, `deploy:check`, `deploy:cloudflare`.
- `npm run lint`: PASS
- `npm run typecheck`: PASS
- `npm run build:cloudflare`: PASS
- `npm test` was not re-run for this docs-only deploy guardrail; last full unit suite in this session passed during Story 1.8.
- CodeRabbit skipped due free CLI rate limit already reached in this session.

### Completion Notes List

- Added explicit build guardrails to `docs/deploy.md`.
- Clarified `npm run build` is a Next.js framework diagnostic build, not the Cloudflare production artifact.
- Clarified `npm run build:cloudflare` produces `.open-next/worker.js` for Workers.
- Kept `npm run deploy:check` as the recommended pre-deploy validation command.
- Did not alter `open-next.config.ts` or `wrangler.jsonc`; no runtime deployment mismatch was found.

### File List

- `docs/deploy.md`
- `docs/stories/1.10.cloudflare-build-path-guardrail.md`

## QA Results

### Review Date: 2026-06-21

### Reviewed By: Quinn (Test Architect)

### Verdict: PASS

Evidence-based review against the real repository state. This is a docs-only guardrail story; its change is the "Guardrails de build" section (+8 lines) in `docs/deploy.md`, already committed to `main` (commit `7b933d4`). No application code or runtime config was touched.

#### Acceptance Criteria Traceability

| AC | Status | Evidence |
| --- | --- | --- |
| AC1 — scripts reviewed for ambiguity | PASS | `package.json` scripts verified: `build`=`next build`, `build:cloudflare`=`opennextjs-cloudflare build`. The diagnostic-vs-production distinction is made explicit in `deploy.md:17-18`. |
| AC2 — docs state Cloudflare uses OpenNext Workers | PASS | `deploy.md:3` ("deploy no Cloudflare deve usar Workers com OpenNext"), reinforced at `:18` and `:57`. |
| AC3 — `deploy:check` remains recommended pre-deploy | PASS | `deploy.md:19`; `package.json` `deploy:check` = `lint && typecheck && test && build:cloudflare`. |
| AC4 — Vercel/static-export wording corrected | PASS | `deploy.md:21` ("Nao use next export, Cloudflare Pages estatico ou Vercel...") and `:57`. |
| AC5 — no runtime config change unless mismatch | PASS | Commit `7b933d4` touched only `docs/deploy.md` (+8). `open-next.config.ts` and `wrangler.jsonc` untouched. |
| AC6 — validation commands documented | PASS | Dev Agent Record documents lint/typecheck/build:cloudflare PASS; `npm test` skipped (docs-only) with reason; CodeRabbit skipped (free CLI rate limit). |

#### 7 Quality Checks

1. **Code review** — PASS. Docs-only; wording matches `package.json` scripts exactly (verified field-by-field). No ambiguity remains between `build` and `build:cloudflare`.
2. **Unit tests** — N/A (no code changed). Full suite green regardless: 41 files / 147 tests PASS (re-run this session).
3. **Acceptance criteria** — PASS. All 6 traced to real code/docs (table above).
4. **No regressions** — PASS. Zero code files touched; runtime config (`open-next.config.ts`, `wrangler.jsonc`) untouched.
5. **Performance** — N/A (documentation change).
6. **Security** — PASS. No attack surface; documentation only.
7. **Documentation** — PASS. This story *is* the documentation guardrail; content is accurate and unambiguous.

#### Non-blocking Observations (low severity)

- **DOC-001 (low):** `deploy.md` is written without diacritics ("producao", "e", "Configuracao"). This matches the existing repo doc convention (e.g., `AGENTS.md` is also accent-free) and was not introduced by this story — out of scope, non-blocking.
- **PROCESS-001 (info):** The story content was already merged to `main` via the Epic 1 docs commit (`7b933d4`) before this QA pass; the story file's status lagged behind reality until now. Recorded for traceability; not a defect.

### Gate Status

Gate: PASS → docs/qa/gates/1.10-cloudflare-build-path-guardrail.yml
