# Story 2.2: Harden payment status lookup

## Status

Ready for Review

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools:
- npm run lint
- npm run typecheck
- npm test

## Story

**As a** buyer checking payment progress,  
**I want** payment status lookups limited to authorized checkout context,  
**so that** payment state is not disclosed by possession of a charge id alone.

## Acceptance Criteria

1. `/api/payments/status/[chargeId]` requires a signed lookup token or equivalent authorization mechanism.
2. A request with only `chargeId` cannot retrieve status.
3. Invalid, expired, or mismatched authorization returns a generic safe error.
4. Tests cover valid token, missing token, invalid token, mismatched charge id, expired token, and unknown charge.
5. If service-role remains necessary, its use is constrained and documented.

## Source of Truth

- `docs/security/site-security-audit-2026-06-21.md`
- `docs/security/payment-status-authorization-design-2026-06-21.md`
- `src/app/api/payments/status/[chargeId]/route.ts`
- `src/app/actions/payment.ts`

## Tasks / Subtasks

- [x] Add server-only payment status token generation/verification utilities.
- [x] Return status lookup authorization from checkout creation flow.
- [x] Require and validate token in the payment status route.
- [x] Add route and utility tests.
- [x] Run required quality gates.

## Dev Notes

- Use HMAC SHA-256 with a server-only secret.
- Token payload must bind `chargeId`, purpose, issued-at, and expiration.
- Keep the response status-only.

## Testing

- Add unit tests for token utility.
- Add route tests for status authorization matrix.
- Run `npm run lint`, `npm run typecheck`, `npm test`.

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-06-21 | 0.1 | Story drafted from PM plan and Architect decision | River |
| 2026-06-21 | 0.2 | PO validation GO, P1 priority confirmed | Pax |
| 2026-06-21 | 1.0 | Implemented signed payment status lookup token and route authorization | Dex |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `npm test -- src/lib/payments/status-token.test.ts 'src/app/api/payments/status/[chargeId]/route.test.ts' src/app/actions/payment.test.ts`: PASS (3 files, 15 tests)
- `npm test -- src/lib/payments/status-token.test.ts 'src/app/api/payments/status/[chargeId]/route.test.ts' src/app/actions/payment.test.ts`: PASS (3 files, 16 tests)
- `npm run lint`: PASS
- `npm run typecheck`: PASS
- `npm test`: PASS (39 files, 140 tests)
- `npm run deploy:check`: PASS (39 files, 140 tests, Cloudflare build complete; rerun outside sandbox after Turbopack sandbox bind restriction)

### Completion Notes List

- Added server-only HMAC SHA-256 payment status tokens bound to `chargeId`, `purpose`, `iat`, and `exp`.
- Checkout creation now returns `statusToken` alongside `chargeId`.
- Checkout creation validates `PAYMENT_STATUS_TOKEN_SECRET` before Supabase or Asaas side effects.
- `/api/payments/status/[chargeId]` rejects missing, invalid, expired, or mismatched tokens before creating the service-role client.
- Service-role read remains constrained to the token-validated charge id and returns only `{ status }`.

### File List

- `.env.example`
- `docs/stories/2.2.harden-payment-status-lookup.md`
- `src/app/actions/payment.ts`
- `src/app/actions/payment.test.ts`
- `src/app/api/payments/status/[chargeId]/route.ts`
- `src/app/api/payments/status/[chargeId]/route.test.ts`
- `src/lib/payments/status-token.ts`
- `src/lib/payments/status-token.test.ts`

## QA Results

_To be filled by QA._
