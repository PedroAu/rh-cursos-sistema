# Story 1.7: Add E2E coverage for critical RH Cursos flows

## Status

Approved

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools:
- npm run lint
- npm run typecheck
- npm test
- npm run build:cloudflare

## Story

**As a** maintainer preparing production changes,  
**I want** Playwright E2E coverage for critical user and admin flows,  
**so that** regressions in login, enrollment, and checkout are caught before deployment.

## Acceptance Criteria

1. An E2E test location exists and is wired into the existing Playwright configuration.
2. At least one login/admin access flow is covered.
3. At least one public lead or enrollment flow is covered.
4. Pix/Boleto checkout behavior is covered at a safe level using mocks or test-mode assumptions; no real charge is required.
5. Tests are documented with required environment/test data setup.
6. `npm run test:e2e` runs the new specs locally or documents any required external dependency that blocks execution.

## Source of Truth

- `docs/architecture/system-architecture.md#Testing Reality`
- `playwright.config.ts`
- Existing unit tests near payment and public forms

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Testing  
**Secondary Type(s)**: Integration  
**Complexity**: Medium

### Specialized Agent Assignment

**Primary Agents**:
- @dev
- @qa

**Supporting Agents**:
- @architect only if a new e2e test architecture/convention is required
- @devops only for remote push/PR after local completion

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): validate new e2e specs run via Playwright and reuse existing specs without duplication before marking story ready for review.
- [ ] Pre-Review (@qa): verify story acceptance criteria and that no production Asaas/Supabase endpoints are exercised.
- [ ] Pre-PR (@devops): run repository quality gates before creating a pull request.

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (full mode per `coderabbit_integration.self_healing`)
- Max Iterations: 3
- Timeout: 30 minutes
- Severity Filter: CRITICAL and HIGH auto-fix; MEDIUM documented as debt; LOW ignored

**Predicted Behavior**:
- CRITICAL issues: auto-fix before story completion
- HIGH issues: auto-fix before story completion

### CodeRabbit Focus Areas

- Reuse the existing e2e specs (`login.spec.ts`, `enrollment.spec.ts`, `a11y.spec.ts`, `smoke.spec.ts`) instead of recreating them; new work must extend, not duplicate, this structure.
- Never hit production Asaas or production Supabase from any spec.
- Use deterministic mocks/test fixtures for Pix/Boleto checkout coverage; no live payment gateway calls.
- No real charge may be created as a side effect of any test run.
- Document any required env vars or test data needed to run the new checkout coverage.

## Tasks / Subtasks

- [ ] Inspect existing Playwright config.
- [ ] Add E2E folder/spec structure.
- [ ] Define safe test data and mocking strategy.
- [ ] Add login/admin access coverage.
- [ ] Add public lead/enrollment coverage.
- [ ] Add Pix/Boleto checkout coverage.
- [ ] Document how to run E2E tests.
- [ ] Run required quality gates, including `npm run test:e2e` if environment permits.

## Dev Notes

- Do not hit production Asaas or production Supabase.
- Prefer deterministic mocks or local test fixtures.

### Existing E2E Coverage (do not recreate)

- `e2e/login.spec.ts` (35 lines), `e2e/enrollment.spec.ts` (130 lines, navigates a real course slug to `/inscricao`), `e2e/a11y.spec.ts` (57 lines), and `e2e/smoke.spec.ts` (6 lines) already exist and are wired into `playwright.config.ts` (`testDir: "./e2e"`). `npm run test:e2e` runs `playwright test` against this folder. [Source: verified directly in `e2e/` and `playwright.config.ts`]
- This satisfies AC#1 (location exists + wired), AC#2 (login/admin access flow), and AC#3 (public enrollment flow) as-is.
- The remaining gap is AC#4: Pix/Boleto checkout coverage does not exist yet. Implementation must add this coverage using deterministic mocks/test-mode assumptions, with no real charge created.
- REUSE > CREATE: extend the existing `e2e/` structure and conventions (spec naming, fixtures, viewport handling) rather than rebuilding login/enrollment/a11y/smoke coverage from scratch.

## Testing

- Reuse the existing e2e/ folder and playwright.config.ts; do not recreate already-covered flows.
- Add deterministic-mock coverage for Pix/Boleto checkout (no real charge, no production endpoints).
- Run `npm run test:e2e` locally, or document any external dependency that blocks execution.

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-06-19 | 0.1 | Initial draft from Architect brownfield handoff | River |
| 2026-06-19 | 0.2 | Dev Notes updated to reflect existing e2e specs (REUSE) | River |
| 2026-06-19 | 0.3 | Expanded to canonical template (Executor Assignment, CodeRabbit Integration, Testing, Dev Agent Record) | River |
| 2026-06-19 | 0.4 | Validated GO (9/10) — PO gate passed; anti-hallucination corrected (existing e2e specs); Status: Approved | @po (Pax) |

## Dev Agent Record

### Agent Model Used

_To be filled by Dev Agent._

### Debug Log References

_To be filled by Dev Agent._

### Completion Notes List

_To be filled by Dev Agent._

### File List

_To be filled by Dev Agent._

## QA Results

_To be filled by QA._
